// Lógica de generación de archivos
import fs from 'fs-extra';
import path from 'path';

export async function generateProject(options: any) {
  // TODO: Implement file generation logic
  console.log("Generating project with options:", options);
}
