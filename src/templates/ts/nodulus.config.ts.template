import type { NodulusConfig } from 'nodulus-core'

const config: NodulusConfig = {
  modules: 'src/modules/*',
  prefix: '/api/',
  strict: process.env.NODE_ENV !== 'production',
}

export default config
